#ifndef ENUM_DEFINITIONS
#define ENUM_DEFINITIONS

//enum definitions
enum gameScreens { entrance, help, game, scoreboard, youDied, end }; //make enum for all the screens
enum blockTypes { instantDeath, normal, quadrupleScore, bigBounce, tenTimeScore}; //enums for the block types

#endif